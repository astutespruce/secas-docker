# binary deps that use GDAL
# last updated 10/27/2023

# Fiona==1.8.21  # no longer used; specifically excluded from geopandas
pyogrio==0.7.1
rasterio==1.3.7

# pymgl==0.3.1  # must be installed in Dockerfile directly from wheel in Dockerfile