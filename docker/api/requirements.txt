# last updated 10/19/2022

### Shared dependencies
affine==2.3.1
aiofiles==22.1.0
arq==0.25.0
fastapi==0.89.1
# geopandas==0.11.1  # installed separately, without dependencies
gunicorn==20.1.0
h11==0.12 # required to meet other dependency requirements
pandas==1.5.2
progress==1.6
pyarrow==10.0.1
python-dotenv==0.20.0
python-multipart==0.0.5
sentry-sdk==1.14.0
uvicorn[standard]==0.20.0

### Dependencies for geopandas, since we install it without them to avoid Fiona
pyproj==3.4.0

### Dependencies for Southeast Blueprint Explorer
jinja2==3.1.2
Pillow==9.4.0
# pymgl: installed manually due to lack of manylinux wheels
WeasyPrint==57.2

### Dependencies for Southeast Species Status Landscape Assessment Tool
openpyxl==3.0.10