# Python dependencies that must be installed to install other dependencies

Cython==0.29.30
numpy==1.22.4
pygeos==0.12.0

# flit_core is needed for building fiona/rasterio in this env
flit_core==3.7.1
